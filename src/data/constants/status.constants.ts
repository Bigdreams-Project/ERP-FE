/**
 * Status and Type Constants
 * Extracted from mock data to speed up compilation
 * These are small constants that don't need the entire 3000+ line mock file
 */

export const courseTypes = ["TecTerminal", "ApTech", "CPMS"];

export const courseStatus = ["Active", "Inactive", "Draft"];

export const studentStatus = ["Active", "Dropout", "Graduated", "On Hold"];

export const batchStatus = ["Active", "Inactive", "Draft"];

