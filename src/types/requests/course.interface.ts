export interface CreateCourse {
  id: string;
  title: string;
  note: string;
  createdAt: string;
  updatedAt: string;
}

export interface UpdateCourse {
  id: string;
  title: string;
  note: string;
  createdAt: string;
  updatedAt: string;
}
