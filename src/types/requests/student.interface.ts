export interface CreateStudent {
  id: string;
  title: string;
  note: string;
  createdAt: string;
  updatedAt: string;
}

export interface UpdateStudent {
  id: string;
  title: string;
  note: string;
  createdAt: string;
  updatedAt: string;
}
