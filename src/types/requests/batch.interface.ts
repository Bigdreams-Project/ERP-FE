export interface CreateBatch {
  id: string;
  title: string;
  note: string;
  createdAt: string;
  updatedAt: string;
}

export interface UpdateBatch {
  id: string;
  title: string;
  note: string;
  createdAt: string;
  updatedAt: string;
}
